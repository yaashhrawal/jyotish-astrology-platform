# Copy to .env.development (local) or set in your host/CI.
# Base URL of the FastAPI backend. Local dev default below.
VITE_API_URL=http://localhost:8888

# Optional: set for demo builds
# VITE_ENV=demo
# VITE_DEMO_MODE=true
